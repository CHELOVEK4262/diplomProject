<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Статистика</title>
    <style>
        body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
}

.container {
    display: flex;
    height: 100vh;
}

/* Боковое меню */
.sidebar {
    width: 220px;
    background-color: #e9e9e9;
    padding-top: 20px;
    box-shadow: 2px 0 5px rgba(0,0,0,0.1);
}

.sidebar-item {
    padding: 15px 20px;
    cursor: pointer;
}

.sidebar-item:hover,
.sidebar-item.active {
    background-color: #ccc;
    font-weight: bold;
}

/* Основной контент */
.main-content {
    flex: 1;
    padding: 20px 40px;
    position: relative;
}

/* Верхняя правая кнопка */
.top-right {
    position: absolute;
    top: 20px;
    right: 20px;
}

.back-button {
    padding: 6px 12px;
    background-color: #d9534f;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

/* Карточки */
.stats-cards {
    display: flex;
    gap: 100px;
    margin-top: 40px;
    margin-bottom: 30px;
    flex-direction: row;
    justify-content: center;
}

.card {
    background: white;
    padding: 20px;
    width: 160px;
    height: 100px;
    text-align: center;
    border-radius: 8px;
    box-shadow: 0 0 5px rgba(0,0,0,0.1);
}

.card-number {
    font-size: 32px;
    font-weight: bold;
    color: #333;
}

.card-label {
    font-size: 14px;
    color: #666;
    margin-top: 8px;
}

/* Фильтр */
.filter {
    margin-bottom: 20px;
}

.filter label {
    margin-right: 10px;
}

.filter input {
    margin-right: 20px;
}

/* Заглушка для графика */
.chart-placeholder {
    background-color: white;
    height: 400px;
    border-radius: 8px;
    box-shadow: 0 0 5px rgba(0,0,0,0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #888;
    font-size: 18px;
}
    </style>
</head>
<body>
    <div style="text-align: right; margin-bottom: 10px; position: fixed; width: 98vw; z-index: 2000; padding-top: 10px;">
    <form action="/admin">
        <button type="submit" style="padding: 6px 12px; background-color: #d9534f; color: white; border: none; border-radius: 4px; cursor: pointer;">
            Вернуться в админ-панель
        </button>
    </form>
</div>
<div class="container">

        <!-- Боковое меню -->
        <aside class="sidebar">
            <div class="sidebar-item active">Главная</div>
            <div class="sidebar-item">Отчет по рейсам</div>
            <div class="sidebar-item">Отчет по транспорту</div>
            <div class="sidebar-item">Отчет по средней стоимости рейса</div>
        </aside>

        <!-- Основная панель -->
        <main class="main-content">
            <!-- Информационные карточки -->
            <div class="stats-cards">
                <div class="card">
                    <div class="card-number"><%= users.length %></div>
                    <div class="card-label">Зарегистрированные<br>пользователи</div>
                </div>
                <div class="card">
                    <div class="card-number"><%= trucks.length %></div>
                    <div class="card-label">Транспортные<br>средства</div>
                </div>
                <div class="card">
                    <div class="card-number"><%= routes.length %></div>
                    <div class="card-label">Добавленные<br>маршруты</div>
                </div>
            </div>

            <!-- Фильтр по дате -->
            <div class="filter">
                <label for="date-from">Период с</label>
                <input type="date" id="date-from">
                <label for="date-to">по</label>
                <input type="date" id="date-to">
            </div>

            <!-- График -->
            <div class="chart-placeholder">
                <!-- Здесь будет график -->
                <div class="chart-simulation">[График динамики по месяцам]</div>
            </div>
        </main>

    </div>
</body>
</html>